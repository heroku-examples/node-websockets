/// <reference types="node" />
import Long from 'long';
import { IGetReturn } from '../types';
/**
 * Decode Unsigned Int 64
 * @param buffer {Buffer}
 * @param offset {Number}
 * @returns {{value: Number, length: Number}}
 */
export declare function getU64(buffer: Buffer, offset?: number): IGetReturn<Long>;
