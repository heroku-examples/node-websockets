/// <reference types="node" />
import Long from 'long';
import { IGetReturn } from '../types';
/**
 * Decode Int 64
 * @param buffer {Buffer}
 * @param offset {Number}
 * @returns {{value: Number, length: Number}}
 */
export declare function get64(buffer: Buffer, offset?: number): IGetReturn<Long>;
