/// <reference types="node" />
import { TYPE } from "../../constants";
/**
 * Encode Variant
 * By default it can Number, Array, Object, Null, String for complex type you need to pass the type
 * @param {*} value
 * @param {TYPE} type
 * @returns {*}
 */
export declare function putVar(value: any, type?: TYPE): Buffer;
